<div class="col  mb-3">
  <a href="{% if project.redirect %}{{ project.redirect }}{% else %}{{ project.url | relative_url }}{% endif %}">
    <div class="card-rline h-100 hoverable">
      {% if project.img %}
        {%
          include figure.liquid
          loading="eager"
          path=project.img
          sizes = "250px"
          alt="project thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        {%  assign pnumber = project.title | split: " "  %}
        <p class="card-text"><strong>{{ pnumber[1]}}</strong> {{ project.subtitle }} <i class="arrow right"></i></p>
      </div>
    </div>
  </a>
</div>
